{{- range tuple "a" "b" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_chart.fullname" $ }}-worker-{{ . }}
  labels:
    {{- include "helm_chart.labels" $ | nindent 4 }}
spec:
  type: {{ $.Values.service.type }}
  ports:
    - port: {{ $.Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_chart.selectorLabels" $ | nindent 4 }}-worker
{{- end }}

{{- range tuple "a" "b" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_chart.fullname" $ }}-redis-{{ . }}
  labels:
    {{- include "helm_chart.labels" $ | nindent 4 }}
spec:
  type: {{ $.Values.service.type }}
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    {{- include "helm_chart.selectorLabels" $ | nindent 4 }}
{{- end }}

{{- range tuple "a" "b" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_chart.fullname" $ }}-web-api-{{ . }}
  labels:
    {{- include "helm_chart.labels" $ | nindent 4 }}
spec:
  type: {{ $.Values.service.type }}
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: api
  selector:
    {{- include "helm_chart.selectorLabels" $ | nindent 4 }}-web-api
{{- end }}

{{- range tuple "a" "b" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_chart.fullname" $ }}-webserver-{{ . }}
  labels:
    {{- include "helm_chart.labels" $ | nindent 4 }}
spec:
  type: {{ $.Values.service.type }}
  ports:
    - port: {{ $.Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_chart.selectorLabels" $ | nindent 4 }}-webserver
{{- end }}
